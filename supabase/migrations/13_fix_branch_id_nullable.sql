-- ============================================================
-- إصلاح جدول assessments - جعل branch_id اختياري
-- ============================================================
-- التاريخ: 2024-12-26
-- السبب: السماح بإنشاء تقارير دخول بدون تحديد فرع
-- ============================================================

-- جعل branch_id nullable في assessments
ALTER TABLE assessments 
ALTER COLUMN branch_id DROP NOT NULL;

-- جعل branch_id nullable في job_orders
ALTER TABLE job_orders 
ALTER COLUMN branch_id DROP NOT NULL;

-- ============================================================
-- ملاحظة: هذا لا يؤثر على البيانات الموجودة
-- يسمح فقط بإنشاء سجلات جديدة بدون تحديد فرع
-- ============================================================
